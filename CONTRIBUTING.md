# 기여 가이드 (Contributing Guide)

이 저장소는 **AI Company Factory** 템플릿입니다. 에이전트 템플릿, 훅, 스킬 변경을 제안하기 전에 이 문서를 읽어주세요.

---

## 시작 전 확인

대규모 변경 전 반드시 아래 순서로 아키텍처를 파악하세요.

1. `ARCHITECTURE.md` — 전체 시스템 구조와 레이어 설명
2. `HARNESS-GUIDE.md` — Claude Code 하네스 동작 방식 및 훅 설계 원칙

이 문서를 먼저 읽지 않고 제출된 PR은 리뷰 없이 반려될 수 있습니다.

---

## 로컬 설정

```bash
bash install.sh
```

설치 후 `.claude/` 및 `.omc/` 디렉터리가 생성됩니다.

---

## 브랜치 및 PR 규칙

| 작업 유형 | 절차 |
|---|---|
| `feat:` / `fix:` | Issue 먼저 → `feature/#N-slug` 브랜치 → PR |
| `refactor:` / `docs:` / `chore:` | `main` 직접 커밋 허용 |

### feat/fix 흐름 예시

```
1. GitHub Issue 생성 (#42 — 새 에이전트 템플릿 추가)
2. 브랜치 생성: feature/#42-add-researcher-agent
3. 작업 후 PR 오픈 → CodeRabbit 자동 리뷰 대기
4. 메인테이너 승인 후 머지
```

---

## 커밋 접두사 (원자적 커밋)

| 접두사 | 사용 시점 |
|---|---|
| `feat:` | 새 기능, 새 에이전트/훅/스킬 추가 |
| `fix:` | 버그 수정 |
| `refactor:` | 동작 변경 없는 코드 구조 개선 |
| `docs:` | 문서만 변경 |
| `chore:` | 빌드, 의존성, 설정 파일 변경 |

커밋 하나에 하나의 논리적 변경만 담으세요. 여러 관심사를 하나의 커밋에 섞지 마세요.

---

## 보안 규칙 (절대 금지)

- API 키, 토큰, 비밀번호를 코드에 하드코딩하지 마세요.
- `.env` 파일을 커밋하지 마세요. `.gitignore`에 반드시 포함되어야 합니다.
- `cat .env` 명령으로 터미널에 키값을 노출하지 마세요.
- 커밋 전 `git status`로 민감 파일 포함 여부를 확인하세요.
- 시크릿 패턴 감지 시 즉시 작업을 중단하고 경고를 남기세요: `AKIA...`, `sk-...`, `ghp_...`

---

## 에이전트 템플릿 / 훅 / 스킬 변경 제안

1. `template_change` Issue 템플릿을 사용해 변경 유형, 근거, 전파 계획을 작성하세요.
2. `ARCHITECTURE.md`와 `HARNESS-GUIDE.md`를 참고해 기존 패턴과 충돌하지 않는지 확인하세요.
3. 다운스트림 템플릿에 영향을 주는 변경은 `sync-manifest.json` 갱신 계획을 PR에 명시하세요.

---

## PR 리뷰 기대사항

- **CodeRabbit**이 자동으로 PR을 리뷰합니다. 리뷰 코멘트에 응답해주세요.
- 메인테이너 최소 1명의 승인이 머지에 필요합니다.
- CI가 실패하면 머지할 수 없습니다.
